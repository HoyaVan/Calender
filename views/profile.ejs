<!DOCTYPE html>
<html lang="en" class="h-full">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
  <link rel="stylesheet" type="text/css" href="/output.css"/>
</head>

<body class="min-h-full bg-gray-50 <%= locals.authenticated ? 'authenticated' : '' %>"
  data-authenticated="<%= locals.authenticated %>">
  <header class="bg-white border-b">
    <div class="max-w-5xl mx-auto px-4 py-6 flex items-center justify-between">
      <div>
        <h1 class="text-2xl font-semibold">
          Hello, <span class="text-blue-600">
            <%= displayName %>
          </span>!
        </h1>
        <p class="text-gray-500">Welcome to your profile. Manage your friends and events below.</p>
      </div>

      <div class="flex items-center space-x-3">
        <!-- Profile -->
        <a href="/profile"
          class="h-10 w-10 flex items-center justify-center rounded-full bg-blue-500 text-white hover:bg-blue-600 transition"
          title="Profile">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" class="w-5 h-5"
            stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
            <circle cx="12" cy="7" r="4"></circle>
          </svg>
        </a>

        <!-- Home -->
        <a href="/"
          class="h-10 w-10 flex items-center justify-center rounded-full bg-gray-800 text-white hover:bg-black transition"
          title="Home">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" class="w-5 h-5"
            stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M3 9.5L12 3l9 6.5V21a1 1 0 0 1-1 1h-5v-6H9v6H4a1 1 0 0 1-1-1z" />
          </svg>
        </a>

        <!-- Log Out -->
        <form action="/logout" method="post" class="inline">
          <button type="submit"
            class="h-10 w-10 flex items-center justify-center rounded-full bg-red-500 text-white hover:bg-red-600 transition"
            title="Log Out">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2"
              stroke="currentColor" class="w-5 h-5">
              <path stroke-linecap="round" stroke-linejoin="round"
                d="M15.75 9V5.25A2.25 2.25 0 0 0 13.5 3h-6A2.25 2.25 0 0 0 5.25 5.25v13.5A2.25 2.25 0 0 0 7.5 21h6a2.25 2.25 0 0 0 2.25-2.25V15M18 12H9m9 0l-3 3m3-3l-3-3" />
            </svg>
          </button>
        </form>
      </div>
    </div>
  </header>

  <main class="max-w-5xl mx-auto px-4 py-8">
    <!-- Success/Error Messages -->
    <% if (typeof success !== 'undefined' && success) { %>
      <div class="mb-6 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
        <span class="block sm:inline"><%= success %></span>
      </div>
    <% } %>
    <% if (typeof error !== 'undefined' && error) { %>
      <div class="mb-6 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
        <span class="block sm:inline"><%= error %></span>
      </div>
    <% } %>

    <!-- Friend Invite Section -->
    <section class="mb-8 bg-white rounded-lg shadow-sm ring-1 ring-gray-200 p-6">
      <h2 class="text-xl font-semibold text-gray-900 mb-4">Send Friend Request</h2>
      <div class="mb-4">
        <form id="searchForm" class="flex gap-2">
          <input 
            type="text" 
            id="searchInput" 
            placeholder="Search by username or email..." 
            class="flex-1 px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
          <button 
            type="submit" 
            class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition"
          >
            Search
          </button>
        </form>
      </div>
      <div id="searchResults" class="mt-4"></div>
    </section>

    <!-- Pending Friend Requests Section -->
    <section class="mb-8 bg-white rounded-lg shadow-sm ring-1 ring-gray-200 p-6">
      <h2 class="text-xl font-semibold text-gray-900 mb-4">Pending Friend Requests</h2>
      <% const pendingRequestsList = (typeof pendingRequests !== 'undefined' && pendingRequests && pendingRequests.length) ? pendingRequests : []; %>
      <% if (pendingRequestsList.length === 0) { %>
        <p class="text-gray-500">No pending friend requests.</p>
      <% } else { %>
        <div class="space-y-3">
          <% for (const request of pendingRequestsList) { %>
            <div class="flex items-center justify-between p-4 bg-gray-50 rounded-lg">
              <div>
                <p class="font-medium text-gray-900"><%= request.username %></p>
                <p class="text-sm text-gray-500"><%= request.email %></p>
              </div>
              <div class="flex gap-2">
                <form action="/friends/accept-request" method="POST" class="inline">
                  <input type="hidden" name="requestUserId" value="<%= request.request_user_id %>">
                  <button 
                    type="submit" 
                    class="px-4 py-2 bg-green-600 text-white rounded-md hover:bg-green-700 transition"
                  >
                    Accept
                  </button>
                </form>
                <form action="/friends/reject-request" method="POST" class="inline">
                  <input type="hidden" name="requestUserId" value="<%= request.request_user_id %>">
                  <button 
                    type="submit" 
                    class="px-4 py-2 bg-red-600 text-white rounded-md hover:bg-red-700 transition"
                  >
                    Reject
                  </button>
                </form>
              </div>
            </div>
          <% } %>
        </div>
      <% } %>
    </section>

    <!-- Sent Friend Requests Section -->
    <section class="mb-8 bg-white rounded-lg shadow-sm ring-1 ring-gray-200 p-6">
      <h2 class="text-xl font-semibold text-gray-900 mb-4">Sent Friend Requests</h2>
      <% const sentRequestsList = (typeof sentRequests !== 'undefined' && sentRequests && sentRequests.length) ? sentRequests : []; %>
      <% if (sentRequestsList.length === 0) { %>
        <p class="text-gray-500">No sent friend requests.</p>
      <% } else { %>
        <div class="space-y-3">
          <% for (const request of sentRequestsList) { %>
            <div class="flex items-center justify-between p-4 bg-gray-50 rounded-lg">
              <div>
                <p class="font-medium text-gray-900"><%= request.username %></p>
                <p class="text-sm text-gray-500"><%= request.email %></p>
              </div>
              <span class="px-3 py-1 bg-yellow-100 text-yellow-800 rounded-full text-sm">Pending</span>
            </div>
          <% } %>
        </div>
      <% } %>
    </section>

    <!-- Friends List Section -->
    <section class="mb-8 bg-white rounded-lg shadow-sm ring-1 ring-gray-200 p-6">
      <h2 class="text-xl font-semibold text-gray-900 mb-4">My Friends</h2>
      <% const friendsList = (typeof friends !== 'undefined' && friends && friends.length) ? friends : []; %>
      <% if (friendsList.length === 0) { %>
        <p class="text-gray-500">No friends yet. Send friend requests to get started!</p>
      <% } else { %>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          <% for (const friend of friendsList) { %>
            <div class="p-4 bg-gray-50 rounded-lg border border-gray-200">
              <p class="font-medium text-gray-900"><%= friend.username %></p>
              <p class="text-sm text-gray-500"><%= friend.email %></p>
            </div>
          <% } %>
        </div>
      <% } %>
    </section>

    <!-- Events Section (keeping existing events display) -->
    <section class="bg-white rounded-lg shadow-sm ring-1 ring-gray-200 p-6">
      <h2 class="text-xl font-semibold text-gray-900 mb-4">My Events</h2>
      <% const myEvents = (typeof events !== 'undefined' && events && events.length) ? events : []; %>
      <% if (myEvents.length === 0) { %>
        <div class="rounded-lg border border-dashed border-gray-300 bg-white p-10 text-center">
          <p class="text-gray-600">No events yet.</p>
          <a href="/addEvent" class="inline-block mt-3 text-blue-600 hover:underline">Create your first event</a>
        </div>
      <% } else { %>
        <div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
          <% for (const event of myEvents) { %>
            <article class="bg-gray-50 rounded-lg border border-gray-200 p-4">
              <h3 class="text-gray-900 font-semibold mb-2"><%= event.event_name || event.title %></h3>
              <% if (event.event_start) { %>
                <p class="text-sm text-gray-600">Start: <%= new Date(event.event_start).toLocaleString() %></p>
              <% } %>
              <% if (event.event_end) { %>
                <p class="text-sm text-gray-600">End: <%= new Date(event.event_end).toLocaleString() %></p>
              <% } %>
            </article>
          <% } %>
        </div>
      <% } %>
    </section>
  </main>

  <!-- Include Footer -->
  <%- include('templates/footer') %>

  <script>
    // Handle user search
    document.getElementById('searchForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      const searchTerm = document.getElementById('searchInput').value.trim();
      const resultsDiv = document.getElementById('searchResults');

      if (!searchTerm) {
        resultsDiv.innerHTML = '<p class="text-gray-500">Please enter a search term.</p>';
        return;
      }

      resultsDiv.innerHTML = '<p class="text-gray-500">Searching...</p>';

      try {
        const response = await fetch('/friends/search', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
          },
          body: `searchTerm=${encodeURIComponent(searchTerm)}`
        });

        const data = await response.json();

        if (data.success && data.users.length > 0) {
          let html = '<div class="space-y-2">';
          data.users.forEach(user => {
            html += `
              <div class="flex items-center justify-between p-3 bg-gray-50 rounded-lg">
                <div>
                  <p class="font-medium text-gray-900">${user.username}</p>
                  <p class="text-sm text-gray-500">${user.email}</p>
                </div>
                <form action="/friends/send-request" method="POST" class="inline">
                  <input type="hidden" name="receiveUserId" value="${user.user_id}">
                  <button 
                    type="submit" 
                    class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition text-sm"
                  >
                    Send Request
                  </button>
                </form>
              </div>
            `;
          });
          html += '</div>';
          resultsDiv.innerHTML = html;
        } else {
          resultsDiv.innerHTML = '<p class="text-gray-500">No users found.</p>';
        }
      } catch (error) {
        console.error('Search error:', error);
        resultsDiv.innerHTML = '<p class="text-red-500">Error searching users. Please try again.</p>';
      }
    });
  </script>
</body>

</html>
