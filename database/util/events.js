/**
 * Get all events
 * @returns {Promise<Array>} Array of event objects (stub - returns empty array for now)
 */
async function getAllEvents() {
  // Stub function - returns empty array for now
  return [];
}

module.exports = {
  getAllEvents
};
